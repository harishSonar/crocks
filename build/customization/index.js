const PATH = require('path');
const ROOT_PATH = PATH.resolve(__dirname, '../../');

module.exports = {
    // Customization aliases
    // 'digitalexp-light-sdk/lib/sdk/sdkReducers': PATH.resolve(ROOT_PATH, 'src/Application.reducers')
};
